// implements abstract signer class

import { Signer } from "ethersv5";

export async function getAbstractSigner(signer: Signer, verbose = true) {
	// TODO: create abstract signer class that is compatible with ethers v5, v6, viem, web3js
	return signer
}
